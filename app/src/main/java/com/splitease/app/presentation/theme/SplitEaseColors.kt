package com.splitease.app.presentation.theme

import androidx.compose.ui.graphics.Color

/** Brand color tokens exposed for non-MaterialScheme usages (icons, charts later). */
object SplitEaseColors {
    val Primary = Color(0xFF0F766E)
    val PrimaryContainer = Color(0xFFCCFBF1)
    val Accent = Color(0xFF0E7490)
    val Success = Color(0xFF047857)
    val Warning = Color(0xFFB45309)
}
